import { NoautocompleteDirective } from './noautocomplete.directive';

describe('NoautocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new NoautocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
